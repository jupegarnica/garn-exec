scripts:
  test-watch: deno test --unstable --allow-run --allow-read  --watch
  test: deno test --unstable --allow-run --allow-read
  release: git tag -a 0.3.0 -m "new release" && git push origin master --tags
